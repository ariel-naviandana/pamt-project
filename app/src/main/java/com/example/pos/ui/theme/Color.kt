package com.example.pos.ui.theme

import androidx.compose.ui.graphics.Color

val GreenPrimary = Color(0xFF52C771)
val GreenDark = Color(0xFF1B5E20)
val GreenContainer = Color(0xFFC8E6C9)
val BackgroundLight = Color(0xFFFFFFFF)
val White = Color(0xFFFFFFFF)

// Warna Status Aktif (Kas, Pelanggan, Disetujui, Selesai)
val ActiveStatusText = Color(0xFFC8E6C9)
val ActiveStatusBg = Color(0xFF1EB343)

// Warna Status Nonaktif (Nonaktif, Dibatalkan)
val InactiveStatusText = Color(0xFFFFDAD6) // Merah muda pucat
val InactiveStatusBg = Color(0xFF93000A)   // Merah gelap/marun

// ── WARNA BARU UNTUK STATUS DRAFT ──
val DraftStatusBg = Color(0xFFFFF3E0)   // Oranye sangat muda
val DraftStatusText = Color(0xFFE65100) // Oranye tua yang tegas